name: Python Merge Schedule
on:
  pull_request:
    types:
      - opened
      - edited
  schedule:
    - cron: '*/10 * * * *'

jobs:
  python-merge:
    runs-on: ubuntu-latest
    steps:
      - uses: cs50/python-merge-schedule@v1.0
        token: ${{ secrets.ACCESS_TOKEN }}
        env:
          TIMEZONE: America/New_York
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
